# Local `npm run dev` authenticates against the STAGING lets-roll provider,
# which has localhost:5173/auth/callback registered. API calls to /api are
# proxied to the staging edge by vite.config.js.
VITE_OIDC_AUTHORITY=https://lets-roll.staging.521studios.com
